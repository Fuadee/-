# Templates

โฟลเดอร์นี้ใช้เก็บไฟล์ template `.docx` ที่ backend จะโหลดตอน generate เอกสาร

สำหรับตัวอย่าง `basic_v1`:
- backend จะสร้างไฟล์ `procurement_basic_v1.docx` ให้อัตโนมัติเมื่อเริ่มรันเซิร์ฟเวอร์
- ไม่ commit ไฟล์ `.docx` binary ลง git เพื่อหลีกเลี่ยงข้อจำกัดด้าน PR/review
